{% assign words = text | split: ' ' %}
{% for word in words %}
  {% assign upcase_word = word | upcase %}
  {% if word == upcase_word and word.size > 1 %}
    <strong>{{ word }}</strong>
  {% else %}
    {{ word }}
  {% endif %}
  {% unless forloop.last %} {% endunless %}
{% endfor %}
