{% assign words = text | split: ' ' %}
{% for word in words %}
  {% if word == word | upcase and word.size > 1 %}
    <strong>{{ word }}</strong>
  {% else %}
    {{ word }}
  {% endif %}
  {% unless forloop.last %} {% endunless %}
{% endfor %}
